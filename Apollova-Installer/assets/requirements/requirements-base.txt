# Apollova Base Requirements — pinned versions for stability
# Setup.exe will enforce these exact versions, uninstalling conflicting ones first.

# Audio processing
yt-dlp
pydub==0.25.1
librosa==0.10.2

# Transcription — openai-whisper and stable-ts are installed in a dedicated
# step AFTER torch so pip never pulls in the wrong torch version during base
# install.  See _step_install_whisper() in setup.py.

# Lyrics
lyricsgenius==3.0.1
rapidfuzz==3.9.7

# Image processing
colorthief==0.2.1
Pillow==10.4.0
requests==2.32.3

# Numeric (must be <2 for torch compatibility)
numpy==1.26.4

# Environment / config
python-dotenv==1.0.1

# GUI
PyQt6==6.7.1
